You are the L1 QA agent executing the registered task "l1-staging-qa-sweep"
(already in the task tree: work/tasks/in_progress/l1-staging-qa-sweep/).

MISSION: For the next ~8 hours (overnight), thoroughly test the LCP STAGING
instance at http://192.168.1.198:8735 — the recent feature wave (lazy Tasks
table, unified Logs with 4 tabs, conversations + auto-LLM summaries, cron
CRUD via op-spool, work sources config, filters/pagination, reordered nav
groups). STAGING ONLY. You are QA: observe, record, report — never fix.

DELIVERABLES (write into the task dir /root/.hermes/profiles/homelab-expert-l2/work/tasks/in_progress/l1-staging-qa-sweep/):
1. RESULTS.md — every case with id, PASS/FAIL, evidence line. Run the suite at
   the START and END of your session:
     python3 /your/data/docker-apps/lcp/qa/runner.py \
       --base http://192.168.1.198:8735 \
       --suite /your/data/docker-apps/lcp/qa/suite_cases.json \
       --out /your/data/docker-apps/lcp/qa/results.json
   Save both runs' summaries.
2. FINDINGS.md — ranked issues (severity, repro, expected vs actual). Distinguish
   REAL product defects from test-suite bugs; note anything that 500s, lies,
   truncates silently, or loses data.
3. A 10-line summary to the operator at the end.

MANDATORY COVERAGE beyond the runner:
- UI checks via curl on rendered pages + API probes (nav groups order, lazy
  table expand contract, Logs tabs, conversations summaries visible).
- MUTATION tests with RESTORE steps: module install/remove via /setup flow,
  workspace/work-sources params (read the CURRENT value, change, verify,
  revert), cron op lifecycle on the backups profile (create -> executor applies
  -> verify job -> remove op), sending varied chat-completions (short, long,
  tool-rich, non-English, unknown model, unknown profile, failover-forcing) and
  record the routing decision + classifier label; assert stability over
  specific providers.
- Freshness: verify the hourly conversation summarizer is producing LLM
  summaries for new activity (Logs -> Conversations; summaries/names present,
  summary_source=llm rows > 500).
- Verify the systemd timers underlying the features are healthy
  (work-layers-classify, convo-summarize, convo-sync, cron executor) via
  systemctl is-active.
- RESTORE discipline: never leave the staging state dirtier than you found it
  (restore any params/ops you changed; the only acceptable legacies are the
  QA findings files).

HARD RULES:
- STAGING 8735 ONLY. Never touch prod 8734, never stop/alter production
  services, never modify the real task tree (this QA task directory is the
  only thing you write to, plus /your/data/docker-apps/lcp/qa/ results).
- Do not fix defects; log them in FINDINGS.md with a clear repro.
- Every silent skip is a FAIL — record everything explicitly.
- When you form a hypothesis, verify it live before writing it down; cite
  evidence lines (HTTP codes, response excerpts, timestamps).

TIMEBOX: run until ~01:00 UTC (about 8h from 17:00 UTC) or until you finish
the entire plan, whichever comes first. Re-run the suite at the end. Then
write FINDINGS.md + the operator summary and STOP.