# Environment secrets
.env

# Local gateway config — the tracked template is config/gateway.example.yaml.
# Copy it to config/gateway.yaml on a fresh machine; it's local-only here so it
# never conflicts on pull.
config/gateway.yaml

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
*.egg-info/
.venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Docker volumes

# SQLite databases
data/*.db
data/*.db-shm
data/*.db-wal
lcp_dev_data/

# On-disk secret-key fallback (used when LCP_SECRET_KEY is unset)
data/.lcp_secret_key

# Runtime data (API keys, DB, etc.)
data/*.json
*.db
*.sqlite
