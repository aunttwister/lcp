# Environment secrets
.env

# Legacy local gateway config — config is now DB-backed (seeded in code via
# src/api/config.py SEED_CONFIG); gateway.yaml is obsolete and only kept as a
# local reference.
config/gateway.yaml

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
*.egg-info/
.venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Docker volumes

# SQLite databases
data/*.db
data/*.db-shm
data/*.db-wal
lcp_dev_data/

# On-disk secret-key fallback (used when LCP_SECRET_KEY is unset)
data/.lcp_secret_key

# Runtime data (API keys, DB, etc.)
data/*.json
*.db
*.sqlite
